SHUTTLEAI_API_KEY="" # OPTIONAL, but required for AI to function.
PORT="8080" # OPTIONAL, change the local website port.
USERS_JSON='{"admin":"change-me"}' # OPTIONAL, basic auth users (JSON object of username:password).
SERVER_TIMEOUT_MS="0" # OPTIONAL, 0 disables request timeout for long-lived proxy streams.
KEEP_ALIVE_TIMEOUT_MS="0" # OPTIONAL, 0 disables keep-alive timeout.
